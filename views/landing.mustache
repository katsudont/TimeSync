<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{{title}}</title>
  <!-- Bootstrap CSS -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body style="background-color: #0A1128; color: white; font-family: Arial, sans-serif;">

  <div class="container vh-100 d-flex flex-column justify-content-center align-items-center text-center">
    <!-- Title Section -->
    <h1 class="fw-bold" style="font-size: 3.5rem; margin-bottom: 0.5rem;">TimeSync</h1>
    <p style="font-size: 1.2rem; margin-bottom: 1.5rem; line-height: 1.8; max-width: 600px;">
      {{description}}
    </p>

    <!-- Buttons Section -->
    <div>
      <a href="/register" class="btn btn-primary btn-lg mx-2" style="background-color: #0077b6; border-color: #0077b6; font-size: 1rem; padding: 0.8rem 2rem;">
        Register
      </a>
      <a href="/login" class="btn btn-outline-light btn-lg mx-2" style="color: white; border: 2px solid white; font-size: 1rem; padding: 0.8rem 2rem;">
        Login
      </a>
    </div>

    <!-- Footer Section -->
    <footer style="margin-top: 2rem; font-size: 0.9rem; color: #cbd5e0;">
      <p>&copy; 2024 TimeSync. All rights reserved.</p>
    </footer>
  </div>

  <!-- Bootstrap JS (Optional for future interactivity) -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
